# V2-OBS-RECALL-SUMMARY-STRING-FIX — LIVE RESULTS

## Unit oracle (Node.js harness)

- Post-patch logic: **50/50 PASS** via `tests/recall_summary_unit_tests.mjs`.
- Pre-patch regression (same 50-case matrix against the pre-apply jsCode): **37/50 FAIL** via `tests/recall_summary_prepatch_regression.mjs`. Key failures:
  - A-05 `{success: true}` placeholder → expected `0 rows`, got `1 rows` (classic OBS-RECALL-UX-PREEXISTING).
  - B-11..B-15 one-row recalls → expected `1 row`, got `1 rows`.
  - D-21..D-26 mixed (placeholder + real row) → expected normalised count, got raw count.
  - F-32..F-40 known regression fixtures → all produced `1 rows` on zero-match.

## Live smoke (5 cases)

Executed against live `WF-ME-01` post-apply (versionId `9d1da628-…`).

| Exec | Case | Input | Summary (post-patch) | rr_len | `domain_writes_performed` | Verdict |
|---|---|---|---|---|---|---|
| 7002 | zero-match by bogus category | `{action:recall_memory, category:"zzz_recall_v2037_nomatch_zzz", limit:50}` | `Memory recall completed (0 rows).` | 0 | `false` | **PASS** — was previously `1 rows` due to Postgres `{}` placeholder from `ME_Memory_Recall_DB` (visible in exec 7002 runData). Filter caught it. |
| 7011 | one-row | `{action:recall_memory, category:"db_infra", limit:50}` (known to have exactly 1 active row under the smoke thread) | `Memory recall completed (1 row).` | 1 | `false` | **PASS** — singular. Previously would have been `1 rows`. |
| 7020 | multi-row | `{action:recall_memory, category:"mixed_flow", limit:50}` | `Memory recall completed (3 rows).` | 3 | `false` | **PASS** — plural correct; count derived from normalised list. |
| 7029 | 5-row | `{action:recall_memory, category:"observation", limit:50}` | `Memory recall completed (5 rows).` | 5 | `false` | **PASS** — plural correct; count derived from normalised list. |
| 7038 | no filters (recall with only action) | `{action:recall_memory}` | `Memory recall completed (0 rows).` | 0 | `false` | **PASS** — prep did not short-circuit, tenant-only recall yielded zero rows, summary correctly reports zero. |

Raw exec JSON captured at `artifacts/runtime/exec_{7002,7011,7020,7029,7038}.raw.json` (sizes 100–144 KB each, fetched via direct `GET /api/v1/executions/{id}?includeData=true` using the pack's `.env`).

## DB invariants

- **Recall is pure-read**: `SELECT count(*) FROM memory_items WHERE idempotency_key LIKE '%v2037-recall-%'` → **0 rows** (no writes from any of the 5 smokes). `domain_writes_performed: false` in every envelope.
- 101+ historical NULL-embedding rows unchanged (no backfill).
- Non-target lanes unchanged: no store / supersede / promote / search traffic generated by the smokes.

## Acceptance

All acceptance criteria from `02_MISSION_A_RECALL_SUMMARY_STRING_FIX.md §Acceptance` satisfied:

- The bad `1 rows` summary no longer appears on zero-match (exec 7002 / 7038 prove it).
- Actual 1-row recall says singular `1 row` (exec 7011).
- Zero-result recall does not imply one row (exec 7002 / 7038).
- `nodeCount` and `connectionCount` unchanged (49 / 67).
- Only `ME_Memory_Recall_Result.parameters.jsCode` changed (verified by 48/48 non-target byte-identity).
- No DB schema/index change.
- No non-target lane changed.
- Rollback artifact exists (`artifacts/rollback_recall_result_params.json`).
- Ledger row appended (`V2-037`).
- Active frontier remains `NONE` after closeout.

Verdict: **V2-OBS-RECALL-SUMMARY-STRING-FIX CLOSED SUCCESS.**
