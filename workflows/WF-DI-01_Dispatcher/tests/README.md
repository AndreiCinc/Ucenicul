# tests/

## Purpose

Tests and test results for WF-DI-01 Dispatcher.

## Contents

- `test_families.py` — family-based tests.
- `results/results.json` — machine-readable last run results.
- `results/results.md` — human-readable last run summary.

## Canonicality

- `test_families.py` is the canonical authored test suite.
- `results/` is point-in-time output, not a definition of intended behavior.

## Not source of truth

- Test matrix documentation — no `../docs/WF-DI-01_TEST_MATRIX.md` exists. Tracked in `../state/STATE__WF-DI-01.json` → `missing`.
