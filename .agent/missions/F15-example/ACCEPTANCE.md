# ACCEPTANCE — F15-example

The mission is accepted if:

- The executor writes one documentation file under `docs/architecture/agent_orchestration/`.
- The executor writes `EVIDENCE.md`.
- The executor writes valid `EXECUTOR_DONE.json`.
- No files outside allowed roots are changed.
