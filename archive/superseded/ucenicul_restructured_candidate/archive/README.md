# archive/

Historical, superseded, or duplicate artifacts preserved for reference.

Nothing in this folder is a source of truth. All files are byte-for-byte copies of files present in the original repo.

## Subfolders
- `historical_snapshots/` — closure-time snapshots for WF-EC-01 and WF-OR-01 (as captured under `docs/ucenicul_claude_handoff_hardened/archive/`) and the generic root-level active-stage pointers (unsuffixed AUDIT_REPORT.md, BUILD_REPORT.md, CLOSURE_REPORT.md, CURRENT_STAGE.md, FIX_LOG.md, STATE.json from the handoff root).
- `superseded_packs/` — full nested source packs preserved byte-for-byte:
  - `wf-pl-01_full_source_pack/` — the shipped PL-01 pack (from `workflows/wf-pl-01_full_source_pack/`).
  - `wf-or-01_handoff_nested_source_pack/` — duplicate OR-01 material found nested under `docs/ucenicul_claude_handoff_hardened/ucenicul_claude_handoff_hardened/`.
- `deprecated_docs/` — empty at the moment (no file was classified deprecated by this pass).
- `legacy_root/` — empty at the moment (no file was classified "legacy root" by this pass).

The original repository still contains all of these files; this is a non-destructive parallel copy.
