# workflows/WF-DI-01/ — Dispatcher

**Status (based on existing source docs): closed.** Both ACTIVATED and CLOSED route-map / stage-lock variants exist.

## Contents
- `workflow/` — `WF-DI-01_Dispatcher.json`, `WF-DI-01_blueprint.json`.
- `docs/` — connection map, import patch plan, node map, `08_STAGE_WF-DI-01.md`, ACTIVATED and CLOSED route maps, ACTIVATED and CLOSED stage locks.
- `sql/` — DI SQL from `workflows/sql/di/`.
- `scripts/` — `di_logic.py` (+ pyc).
- `tests/` — `test_families.py`, `results/`.
- `reports/` — AUDIT / BUILD / CLOSURE / CURRENT_STAGE / FIX_LOG / STATE for WF-DI-01.
