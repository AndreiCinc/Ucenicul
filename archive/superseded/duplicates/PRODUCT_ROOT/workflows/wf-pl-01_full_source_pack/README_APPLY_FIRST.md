# WF-PL-01 — Apply First

This pack is a **pre-live source pack** for `WF-PL-01` (Plan Generation).

It is meant to be handed to Claude and then used with user-assisted import into n8n.

## What is inside

- stage candidate docs for `WF-PL-01`
- workflow blueprint JSON
- node and connection maps
- import patch plan
- canonical SQL pack
- Python logic port
- heavy script-level proof suite
- generated test results

## What is NOT claimed

- live import has not been performed yet
- no live runtime proof is claimed
- no 10/10 closure is claimed
- this pack must not be mistaken for a live-verified stage closure

## Recommended next sequence

1. place the source files in the repo
2. import `workflows/WF-PL-01_Plan_Generation.json` into the `WF-PL-01` shell
3. have Claude read the live workflow immediately
4. run V1–V6
5. verify post-test DB drift
6. update the canonical reports honestly
