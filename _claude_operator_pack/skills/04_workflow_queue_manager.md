# Skill: workflow_queue_manager
Purpose: construiește și menține `RUN_QUEUE.md`.
Outputs: queue rows, current status per workflow, retry/quarantine markers.
Rule: niciun workflow detectat în scope nu rămâne în afara queue-ului.
