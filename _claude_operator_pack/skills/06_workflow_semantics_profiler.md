# Skill: workflow_semantics_profiler
Purpose: înțelege ce face workflow-ul din JSON, docs și numele nodurilor înainte de a genera documentație.
Outputs: semantic summary, routing summary, DB/write summary, public behavior notes.
Rule: documentația se scrie după semnificație, nu prin copiere mecanică a JSON-ului.
