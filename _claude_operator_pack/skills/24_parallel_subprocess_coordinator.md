# Skill: parallel_subprocess_coordinator
Purpose: lansează și coordonează task-uri paralele sigure pe workflow-uri independente.
Outputs: subprocess assignments, merge order, conflict handling notes.
Forbidden: concurrent writes to same workflow or shared file.
