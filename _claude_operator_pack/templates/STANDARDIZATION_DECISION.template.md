# STANDARDIZATION_DECISION
workflow_in_scope:
mode:
tier:
objective:
canonical_rule:
keep_rules:
exclude_rules:
output_requested:
allowed_writes:
forbidden_writes:
patching_allowed:
packaging_requested:
